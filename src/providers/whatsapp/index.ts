/**
 * WHATSAPP PROVIDER — PRD §3.10 et §24/§25.
 * Interface principale de contrôle humain : rapports, notifications,
 * validation, commandes, idées personnalisées, demandes de modification.
 * API officielle WhatsApp Business / Meta Cloud.
 *
 * 🚧 Squelette Phase 0. Implémentation prévue en PHASE 8 (PRD §37) :
 * webhook, commandes, rapports, validation.
 * ⚠️ Server-only : ne jamais exposer les tokens WhatsApp.
 */

export class WhatsAppProvider {
  // Phase 8 : webhook + envoi/lecture de messages.
}
