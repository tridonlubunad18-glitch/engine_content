/**
 * TIKTOK PROVIDER — PRD §3.9.
 * Publication sur TikTok via l'API officielle lorsque possible.
 *
 * 🚧 Squelette Phase 0. Implémentation prévue en PHASE 9 (PRD §37).
 * ⚠️ Server-only : ne jamais exposer les tokens TikTok.
 */

export class TikTokProvider {
  // Phase 9 : client API TikTok (env : TIKTOK_*).
}
