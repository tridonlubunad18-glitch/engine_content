/**
 * FACEBOOK PROVIDER — PRD §3.9.
 * Publication sur Facebook via la Meta Graph API lorsque possible.
 * Si l'intégration directe est trop complexe : évaluer un intermédiaire
 * (Buffer, Hootsuite…) sans l'ajouter sans nécessité.
 *
 * 🚧 Squelette Phase 0. Implémentation prévue en PHASE 9 (PRD §37).
 * ⚠️ Server-only : ne jamais exposer les tokens Facebook.
 */

export class FacebookProvider {
  // Phase 9 : client Meta Graph API (env : FACEBOOK_*).
}
