/**
 * DEEPSEEK PROVIDER — PRD §3.6.
 * IA principale : analyse marketing, stratégie, angles, hooks, scripts,
 * variantes, analyse des performances, apprentissage, rapports, WhatsApp.
 *
 * Principe : ne jamais appeler DeepSeek quand une logique déterministe
 * suffit (calculs, tris, statistiques, statuts → code).
 *
 * 🚧 Squelette Phase 0. Implémentation prévue en PHASE 1 (PRD §37).
 * ⚠️ Server-only : la clef DEEPSEEK_API_KEY ne doit jamais fuiter.
 */

export class DeepSeekProvider {
  // Phase 1 : client API DeepSeek (env : DEEPSEEK_API_KEY).
}
