/**
 * ELEVENLABS PROVIDER — PRD §3.7 (Voice Provider → ElevenLabs).
 * Génération de voix off / audio. Provider abstrait : remplaçable par
 * un autre fournisseur de voix sans toucher au Voice Engine.
 *
 * 🚧 Squelette Phase 0. Implémentation prévue en PHASE 3 (PRD §37).
 * ⚠️ Server-only : la clef ELEVENLABS_API_KEY ne doit jamais fuiter.
 */

export class ElevenLabsProvider {
  // Phase 3 : client API ElevenLabs (env : ELEVENLABS_API_KEY).
}
