/**
 * DATABASE — PRD §3.4 / §29 / §30.
 * Couche d'accès aux données (Supabase = mémoire structurée).
 * Reposera sur `@/providers/supabase` et définira les schémas typés
 * des tables (ideas, angles, hooks, scripts, content, templates,
 * visual_styles, assets, voiceovers, renders, publications, metrics,
 * experiments, learnings, reports, logs).
 *
 * 🚧 Squelette Phase 0. Implémentation prévue en PHASE 1 (PRD §37).
 */

export {};
